PORT ?= 8000

install:
	poetry install

lint:
	poetry run flake8

test: poetry run pytest --cov=hexlet_flask_example

check: test lint

run:
	poetry run flask --app example --debug run --port $(PORT)

prod:
	poetry run gunicorn --workers 4 --bind 0.0.0.0:$(PORT) hello_world:app --log-file -
[tool.poetry]
name = "python-flask-example"
version = "0.1.0"
description = "Simple flask app"
authors = ["geekster <kirill.kurtov@piklema.com>"]
license = "ISC"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
Flask = "^3.0.3"
gunicorn = "^23.0.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
